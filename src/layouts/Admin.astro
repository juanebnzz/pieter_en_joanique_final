---
/**
 * Admin shell. Installable as a PWA scoped to /admin/ — the manifest and
 * service worker live in public/admin/ so they're served as static files.
 */
import '../styles/global.css';
import { palette } from '../lib/palette';

interface Props {
  title?: string;
}
const { title = 'Wedding admin' } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
    <meta name="robots" content="noindex, nofollow" />
    <title>{title}</title>

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,500;1,500&family=Jost:wght@400;500;600&display=swap" rel="stylesheet" />
    <link rel="manifest" href="/admin/manifest.webmanifest" />
    <meta name="theme-color" content={palette.olive} />
    <meta name="mobile-web-app-capable" content="yes" />
    <meta name="apple-mobile-web-app-capable" content="yes" />
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent" />
    <meta name="apple-mobile-web-app-title" content="P&J Admin" />
    <link rel="apple-touch-icon" href="/admin/icon-192.png" />
    <link rel="icon" type="image/png" href="/admin/icon-192.png" />
  </head>
  <body class="min-h-dvh bg-surface text-ink antialiased">
    <slot />
    <script>
      if ('serviceWorker' in navigator) {
        window.addEventListener('load', () => {
          navigator.serviceWorker.register('/admin/sw.js', { scope: '/admin/' }).catch(() => {});
        });
      }
    </script>
  </body>
</html>
